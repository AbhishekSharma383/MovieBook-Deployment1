class Seat < ApplicationRecord
    # we need connect our seats with show times, but right now I am keeping it all same for very showtime.
    # belongs_to :showtime
    # available , reserved , selected .
end
